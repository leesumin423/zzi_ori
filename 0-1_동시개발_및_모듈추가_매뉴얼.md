# 📘 (주)동양 자금통합포털 멀티모듈 동시개발 & 탭 추가 매뉴얼 (zzi_ori)

> **문서 목적**: 본 매뉴얼은 `주가 / 공시 / 자금 / 퇴직연금` 등 파트별 독립 모듈로 구성된 자금통합포털(`zzi_ori`)을 여러 개발자/팀이 GitHub를 통해 안전하게 **동시 개발**하고, 기존 코드를 건드리지 않고 **새로운 파트 화면(탭)을 3분 만에 추가**할 수 있도록 안내하는 표준 가이드입니다.

---

## 🏛️ 1. 포털 구조 & 동시 개발이 쉬운 이유

자금통합포털은 **"허브-아일랜드(Hub & Island)"** 구조를 가지고 있습니다.
하나의 거대한 통합 코드베이스(Monolith)가 아니라, **독립된 개별 애플리케이션들**을 중앙 포털(Port 9000)이 `iframe` 탭으로 얹어주는 마이크로 프론트엔드 방식입니다.

```
                           ┌──────────────────────────────────────────────┐
                           │   🌐 (주)동양 자금통합포털 (zzi_ori Portal)   │
                           │        (Flask Hub Server / Port 9000)        │
                           └──────────────────────┬───────────────────────┘
                                                  │
       ┌───────────────────┬──────────────────────┼──────────────────────┬───────────────────┐
       │                   │                      │                      │                   │
 ┌─────┴──────────┐ ┌──────┴───────────┐ ┌────────┴───────────┐ ┌────────┴───────────┐ ┌─────┴──────────┐
 │ 1. 퇴직연금    │ │ 2. 차입금 관리   │ │ 3. 주가/공시      │ │ 4. 월간 자금보고  │ │ 5. (NEW) 신규  │
 │    통합 관리   │ │    시스템         │ │    모니터링       │ │    & 단판 메일    │ │    개발팀 모듈 │
 ├────────────────┤ ├──────────────────┤ ├───────────────────┤ ├───────────────────┤ ├────────────────┤
 │ (Port 8000)    │ │ (Port 8501)      │ │ (Port 5000)       │ │ (Port 9000 내장)  │ │ (Port 8080 등) │
 └────────────────┘ └──────────────────┘ └───────────────────┘ └───────────────────┘ └────────────────┘
```

### ✨ 동시 개발 시 최고의 장점
1. **독립된 폴더 & 독립 포트**: 각 팀은 자신의 폴더(`주가현황`, `차입금관리`, `퇴직연금통합관리시스템` 등)와 전용 포트만 사용하므로 **타 팀 코드와 충돌(Git Conflict) 위험이 제로(0)에 가깝습니다.**
2. **기술 스택 자율성**: 신규 팀은 Flask, Streamlit, FastAPI, React, HTML/JS 등 원하는 기술을 자유롭게 사용하여 개발할 수 있습니다.
3. **독립 데이터베이스**: 파트별 DB(SQLite 등)와 JSON 파일이 분리되어 있어, 데이터 변경으로 인한 타 모듈 영향이 없습니다.

> **2026-07 기준 저장소 편입 현황**: 이 저장소(`zzi_ori`)에는 현재 **주가현황·공시(Port 5000)**, **차입금관리(Port 8501)**, **통합포털(Port 9000)**, **퇴직연금(Port 8000)** 4개 앱의 소스코드가 모두 포함되어 있습니다(퇴직연금은 `주가현황/퇴직연금통합관리시스템/` 경로). 실제 회사 데이터가 담긴 DB·엑셀·비밀번호 파일은 어느 앱이든 `.gitignore`로 제외되어 있어 저장소에는 절대 올라가지 않습니다.
>
> ⚠️ **퇴직연금만 예외적으로 주의가 필요합니다**: 실제 서비스 폴더(`퇴직연금통합관리시스템/`)를 확인해보니, 그 경로 자체가 이 저장소(`zzi_ori`)가 아니라 **다른 GitHub 저장소(`leesumin423/-AI-`)를 추적하는 별도의 git 체크아웃**이었고, 커밋 안 된 로컬 변경사항도 남아있는 상태였습니다. 즉 차입금관리ㆍ통합포털처럼 "저장소 사본 → 배포 스크립트로 동기화" 흐름이 아직 이 앱에는 안전하게 연결되어 있지 않습니다 — 배포_최신코드받기.bat도 현재 퇴직연금은 다루지 않습니다. 이 부분은 별도로 정리가 필요해서, 지금은 **퇴직연금 코드를 고칠 땐 실제 서비스 폴더에서 직접 작업하고, zzi_ori 쪽 사본에는 아직 자동으로 반영되지 않는다**는 점만 우선 알아두세요.

---

## ❓ 0. 가장 궁금해할 질문: "GitHub에 올리면 화면에 자동으로 반영되나요?"

**아니요, 자동이 아닙니다.** 그리고 앱마다 반영되는 방식이 다릅니다 — 이 차이를 모르면 "분명 코드를 고쳤는데 왜 화면이 그대로지?" 하고 헷갈리기 쉬우니 꼭 읽어주세요.

| 앱 | 저장소 안 코드 위치 | 실제 서비스 폴더 | GitHub → 실제 화면 반영 방법 |
|---|---|---|---|
| 주가현황ㆍ공시 | `주가현황/` (저장소 최상위 그 자체) | `주가현황/` (**동일 폴더**) | `git pull` 한 번이면 끝. HTML/JS는 새로고침만 해도 반영, 파이썬(`server.py`) 로직을 고쳤으면 서버 재시작 1번 필요 |
| 차입금관리 | `주가현황/차입금관리/` (저장소 안의 **사본**) | `차입금관리/` (저장소 밖, `주가현황/`과 같은 상위 폴더에 위치) | `git pull`만으로는 반영 안 됨 → **[`배포_최신코드받기.bat`](배포_최신코드받기.bat) 실행 필수** (아래 4장 참고). `.streamlit/config.toml`에 `runOnSave = true`가 켜져 있어 배포 스크립트가 파일을 갱신하면 서버 재시작 없이 자동 반영됨 |
| 통합포털 | `주가현황/통합포털/` (저장소 안의 **사본**) | `통합포털/` (저장소 밖) | 위와 동일하게 **`배포_최신코드받기.bat` 실행 필수**. 이 앱은 재시작이 필요함(안내 메시지 참고) |
| 퇴직연금 | `주가현황/퇴직연금통합관리시스템/` (저장소 안의 사본) | `퇴직연금통합관리시스템/` (별도의 다른 git 저장소로 추적되고 있음 — 아래 경고 참고) | 아직 정리 전. `배포_최신코드받기.bat`도 퇴직연금은 다루지 않음 |

즉 **"주가현황 폴더는 저장소 = 실제 서비스 폴더가 같은 것"**이라 매우 단순하지만, **차입금관리ㆍ통합포털은 저장소 안의 사본과 실제 서비스 폴더가 물리적으로 분리**되어 있어서 중간에 "배포" 단계가 한 번 더 필요합니다. 이렇게 나뉜 이유는 두 앱이 원래 이 저장소가 생기기 전부터 별도 폴더로 운영되고 있었기 때문입니다.

---

## 🌿 2. GitHub 기반 파트별 동시 개발 흐름 (Git Workflow)

여러 명의 개발자가 각자 본인 PC에서 개발 후 GitHub(`https://github.com/leesumin423/zzi_ori`)에 올려서 안전하게 합치는 표준 작업 절차입니다.

### 📥 Step 1. 내 PC로 프로젝트 가져오기 (최초 1회)

개발을 시작할 PC의 터미널(VSCode / Git Bash / CMD)을 열고 실행합니다.

```bash
# 1. GitHub 저장소 클론
git clone https://github.com/leesumin423/zzi_ori.git

# 2. 프로젝트 폴더 진입
cd zzi_ori
```

---

### 🌿 Step 2. 자기 파트 전용 브랜치(Branch) 만들기

> `main` 브랜치에 직접 수정 내용을 commit/push 하지 않고, 반드시 파트별 작업 브랜치를 만들어 작업합니다.

```bash
# 최신 main 코드 반영
git checkout main
git pull origin main

# 파트별 브랜치 생성 및 이동 (예시)
git checkout -b feature/stock-update   # 주가 파트 개발자
# 또는
git checkout -b feature/loan-update    # 차입금 파트 개발자
# 또는
git checkout -b feature/pension-update # 퇴직연금 파트 개발자
```

---

### 💻 Step 3. 자기 파트 폴더에서 개발 & 로컬 테스트

자기 담당 폴더 안에서 코드를 수정하고, 본인 담당 포트 서버만 띄워서 테스트합니다.

- **주가/공시 담당자**: 클론받은 저장소의 `주가현황/` 폴더가 곧 실제 서비스 폴더입니다 — 그 안에서 바로 작업하고 Port 5000으로 테스트합니다.
- **차입금관리 담당자**: 클론받은 저장소 안의 **`주가현황/차입금관리/`** 폴더(실제 서비스 폴더의 사본)에서 작업합니다. 실제 회사 데이터(DB·엑셀)는 `.gitignore`로 빠져 있어 처음엔 비어있으니, 테스트용 더미 데이터를 넣거나 관리자 업로드 화면으로 직접 채워서 Port 8501로 테스트하세요.
- **통합포털 담당자**: 마찬가지로 **`주가현황/통합포털/`** 폴더에서 작업합니다. `hub_config.example.py`를 복사해 `hub_config.py`로 만든 뒤(둘 다 같은 폴더 안) 본인 환경에 맞게 값을 채우고 Port 9000으로 테스트하세요 — `hub_config.py`는 절대 커밋되지 않습니다.
- **퇴직연금 담당자**: 실제 서비스 폴더인 `퇴직연금통합관리시스템/`에서 직접 작업 ➡️ Port 8000 띄우고 테스트 (이 폴더는 현재 zzi_ori가 아닌 별도 GitHub 저장소로 추적되고 있어, 배포 흐름이 아직 다른 3개 앱과 다릅니다 — 0장 경고 참고).

> **중요**: 차입금관리ㆍ통합포털은 **저장소 안의 사본에서 개발 → 커밋 → GitHub 반영**까지만 하면 되고, 실제 서비스 폴더(`차입금관리/`, `통합포털/`)를 직접 건드릴 필요가 없습니다. 실제 서비스 폴더 반영은 4장의 배포 스크립트가 담당자 PC에서 나중에 처리합니다.
>
> **팁**: 자기 파트를 개발할 때는 통합포털(9000번 포트)을 매번 켜지 않고, 본인 파트 서버(예: `http://localhost:5000`)만 직접 접속해서 개발하면 훨씬 빠르고 편리합니다!

---

### 📤 Step 4. 코드 저장 & GitHub 업로드 (Commit & Push)

작업이 완료되면 본인 브랜치로 GitHub에 업로드합니다.

```bash
# 1. 변경된 파일 확인
git status

# 2. 커밋 대상 추가 (본인 폴더 위주)
git add 주가현황/
# 또는 전체 추가
git add .

# 3. 커밋 메시지 작성 (어떤 작업을 했는지 명확히 설명)
git commit -m "feat(stock): 주가 실시간 모달 UI 개선 및 금리 파서 보완"

# 4. GitHub 본인 브랜치로 올리기
git push origin feature/stock-update
```

---

### 🔀 Step 5. GitHub에서 Pull Request (PR) 및 합병 (Merge)

1. [GitHub zzi_ori 저장소](https://github.com/leesumin423/zzi_ori) 접속
2. 상단에 뜨는 **"Compare & pull request"** 버튼 클릭
3. 작업 내역 요약 작성 후 **"Create pull request"** 클릭
4. 검토 후 **"Merge pull request"** ➡️ **"Confirm merge"** 클릭하여 `main` 브랜치에 합병

---

### 🖥️ Step 6. 사내 포털 서버 (10.62.15.197) 배포 반영

서버 PC에서 최신 코드를 실제 서비스 폴더로 반영하는 방법은 **앱마다 다릅니다** (0장 표 참고).

**주가현황ㆍ공시** — 저장소 폴더가 곧 서비스 폴더라 pull 한 번이면 끝입니다.
```bash
cd 주가현황
git pull origin main
# server.py 등 파이썬 코드를 고친 경우에만 서버 재시작
```

**차입금관리ㆍ통합포털** — 저장소 안의 사본과 서비스 폴더가 분리되어 있어, `주가현황` 폴더 안의 **[`배포_최신코드받기.bat`](배포_최신코드받기.bat)** 을 더블클릭 한 번 실행합니다. 이 스크립트가 자동으로:
1. `git pull origin main` (GitHub 최신 코드 받기)
2. `robocopy`로 `주가현황/차입금관리` → 실제 `차입금관리/` 폴더에 코드만 동기화 (실데이터ㆍDBㆍ비밀번호 파일은 절대 덮어쓰지 않음)
3. `주가현황/통합포털` → 실제 `통합포털/` 폴더도 동일하게 동기화

를 순서대로 처리해줍니다. 실행 후 화면에 뜨는 안내대로 차입금관리(8501)는 `runOnSave` 덕분에 재시작 불필요, 통합포털(9000)은 수동 재시작이 필요합니다.

---

## ➕ 3. 새 팀 / 신규 모듈 탭 3분 만에 추가하는 법

새로운 팀(예: HR팀, 회계팀 등)이 완전히 새로운 화면을 만들어 통합포털에 탭으로 추가하는 방법입니다.

### 📝 3단계 추가 가이드

#### 1️⃣ Step 1: 새 모듈 폴더 생성 및 독립 포트 실행
새 파트는 기존 폴더를 수정할 필요 없이 **새 폴더**를 만들고 전용 포트(예: 8090)로 웹 서버를 띄우면 됩니다.

예시: `C:\Users\tyinc\Desktop\ai 개발관련\신규자산관리\`
- 포트: `8090`
- 로컬 접속: `http://localhost:8090` (서버 접속: `http://10.62.15.197:8090`)

---

> **아래 예시는 `통합포털/hub_config.py`ㆍ`통합포털/app.py`의 실제 코드 구조를 그대로 반영한 것입니다** (2026-07 기준). 실제 파일을 열어 최신 줄 번호를 확인하면서 따라 하세요.

#### 2️⃣ Step 2: `hub_config.py`(사본은 `hub_config.example.py`)에 포트 등록

통합포털 설정 파일은 포트 하나당 상수 하나를 씁니다 (`PORT_STOCK_DISCLOSURE`, `PORT_TREASURY`, `PORT_PENSION`처럼).

```python
# hub_config.example.py (실제 값을 채운 hub_config.py는 커밋되지 않음)

PORT_STOCK_DISCLOSURE = 5000   # 주가현황/공시
PORT_TREASURY = 8501           # 차입금관리
PORT_PENSION = 8000            # 퇴직연금
HUB_PORT = 9000                # 통합포털 자체
PORT_ASSET = 8090              # ✨ [신규 추가] 신규 자산관리 모듈
```

---

#### 3️⃣ Step 3: `app.py`의 `portal()` 라우트 — `tabs` 리스트에 항목 추가

통합포털 메인 허브(`통합포털/app.py`)는 `/portal` 라우트(`def portal():`) 안에서 매 요청마다 `tabs` 리스트를 만들어 `portal.html`에 넘깁니다. 이 리스트에 딕셔너리 하나만 추가하면 상단 탭에 즉시 나타납니다.

```python
# 통합포털/app.py, portal() 함수 안 (tabs = [ ... ] 부분)

tabs = [
    {
        'id': 'stock', 'label': '📈 주가', 'type': 'iframe',
        'url': f'http://{host}:{hub_config.PORT_STOCK_DISCLOSURE}/주가현황.html',
    },
    {
        'id': 'disclosure', 'label': '📋 공시', 'type': 'iframe',
        'url': f'http://{host}:{hub_config.PORT_STOCK_DISCLOSURE}/주가현황.html?page=disclosures',
    },
    {
        'id': 'treasury', 'label': '💰 자금', 'type': 'group',
        'children': treasury_children,   # 자금 탭처럼 하위 탭을 여러 개 묶고 싶으면 이 형태 참고
    },
    {
        'id': 'pension', 'label': '🧓 퇴직연금', 'type': 'iframe',
        'url': f'http://{host}:{hub_config.PORT_PENSION}/',
    },
    # ✨ [신규 탭 한 줄 추가]
    {
        'id': 'asset', 'label': '🏢 자산관리', 'type': 'iframe',
        'url': f'http://{host}:{hub_config.PORT_ASSET}/',
    },
]
if allowed_sections is not None:
    tabs = [t for t in tabs if t['id'] in allowed_sections]
```

> 팁: `allowed_sections`는 팀별/개인별 화면 접근권한(RBAC) 필터입니다. 새 탭의 `id`(위 예시의 `'asset'`)를 관리자 화면의 "권한설정"에 그대로 등록해야, 필요한 팀에게만 노출시키는 옵션을 쓸 수 있습니다.
>
> 이제 포털(`http://10.62.15.197:9000/portal`) 접속 시 상단에 **"🏢 자산관리"** 탭이 생기고, 클릭 시 8090 포트 화면이 iframe으로 깔끔하게 표시됩니다!

---

## 📡 4. 파트 간 데이터 공유 및 연동 규칙

서로 다른 독립 파트에서 데이터를 공유하거나 전달할 때 지켜야 할 3가지 연동 규칙입니다.

### 1. 읽기 전용 REST API / JSON 방식 준수 (`/data.json`)
- 각 모듈은 타 모듈이 본인의 데이터를 읽을 수 있도록 읽기 전용 API 엔드포인트(`http://10.62.15.197:5000/api/summary` 또는 `data.json`)를 제공합니다.
- 데이터를 가져다 쓰는 쪽에서는 HTTP `GET` 요청으로 JSON 데이터만 수신합니다.

```python
# 예시: 다른 파트에서 퇴직연금 요약 데이터 조회할 때
import requests

response = requests.get("http://10.62.15.197:8000/api/pension_summary.json")
pension_data = response.json()
```

### 2. 타 모듈 DB 직접 수정 금지 (READ ONLY / WRITE PROHIBITED)
- 주가 파트가 차입금 DB나 퇴직연금 DB에 직접 `UPDATE` / `INSERT` 하는 행위는 금지합니다.
- 데이터 변경이 필요할 경우, 해당 파트 담당자에게 API 엔드포인트를 만들어달라고 요청합니다.

### 3. 공용 헬퍼 및 메일링 모듈 재사용
- 포털 허브 및 공용 모듈(`mailer.py`, `hub_db.py`, `danpan_mail.py` 등)은 루트 또는 `shared/` 공통 폴더에 두고 `sys.path`를 이용해 안전하게 공동 재사용합니다.

---

## 💡 요약 체크리스트 (Quick Summary)

| 상황 | 수행해야 할 작업 | 비고 |
|---|---|---|
| **매일 출근 후 개발 시작 시** | `git checkout main` ➡️ `git pull origin main` ➡️ `git checkout feature/내파트` | 최신 코드 동기화 |
| **새 기능 작성 완료 시** | `git add .` ➡️ `git commit -m "메시지"` ➡️ `git push origin feature/내파트` | 본인 브랜치 업로드 |
| **상사/동료에게 검토 요청 시** | GitHub 웹사이트 접속 ➡️ Pull Request (PR) 생성 | 리뷰 및 main 합병 |
| **새로운 파트 화면 붙일 때** | 새 폴더/포트 생성 ➡️ `hub_config.py` 포트등록 ➡️ `app.py`의 `tabs` 리스트에 항목 추가 | 기존 코드 수정 최소화 |
| **merge된 코드를 실제 화면에 반영할 때 (주가현황ㆍ공시)** | `git pull origin main` | 폴더 자체가 서비스 폴더라 이게 전부 |
| **merge된 코드를 실제 화면에 반영할 때 (차입금관리ㆍ통합포털)** | `주가현황` 폴더의 [`배포_최신코드받기.bat`](배포_최신코드받기.bat) 더블클릭 실행 | pull + robocopy 동기화를 자동 처리, 실데이터ㆍ비밀번호는 절대 안 건드림 |
