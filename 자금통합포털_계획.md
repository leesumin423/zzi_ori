# 자금 통합포털 — 현황 및 개발 이력

> 이 문서는 "허브형 통합포털"(주가·공시 / 자금(차입) / 퇴직연금을 한 화면에서 보는 프로젝트)의
> 현재 상태와 **지금까지의 전체 개발 이력**을 정리한 단일 문서입니다. 예전에는 이 계획과
> `포털_배포_계획.md`(주가현황 앱을 사내 그룹웨어나 별도 React 포털에 얹는 방안 검토 문서)가
> 따로 있었는데, 그 검토들은 전부 폐기되고 **지금의 통합포털(Flask 허브 + iframe) 방식으로
> 확정**되었으므로 그 문서는 삭제하고 실제로 완료된 내용만 이 문서로 옮겼습니다.

## ⚠️ 먼저 알아야 할 것: 이 저장소(`zzi_ori`)엔 `주가현황`만 들어있습니다

통합포털은 아래처럼 **서로 다른 로컬 폴더에 있는 4개의 독립 프로그램**을 허브가 iframe으로
묶어서 보여주는 구조입니다. 이 GitHub 저장소(`zzi_ori`)는 그중 `주가현황` 폴더 하나만
추적(git)하고 있고, 나머지 3개(`차입금관리`, `퇴직연금통합관리시스템`, `통합포털`)는 **git으로
관리되지 않는 로컬 폴더**입니다.

```
C:\Users\tyinc\Desktop\ai 개발관련\
├── 주가현황\              ← 이 저장소(zzi_ori git 연결됨). 포트 5000 (Flask, server.py)
├── 차입금관리\             ← git 없음. 포트 8501 (Streamlit, app.py)
├── 통합포털\               ← git 없음. 포트 9000 (Flask 허브, app.py) — 로그인/승인/RBAC/탭 관리
└── 퇴직연금통합관리시스템\  ← git 없음. 포트 8000 (Flask, run_dashboard.py 계열)
```

## 아키텍처 (허브 + iframe)

- `통합포털/app.py` (Flask, 포트 9000)가 **로그인 · 계정승인 · 접근권한(RBAC) · 상단 탭 메뉴**를
  담당하고, 나머지 화면은 각자 원래 포트로 돌아가는 걸 **iframe**으로 그대로 보여줍니다.
- 각 서브앱은 새로 만들지 않고 **기존 코드를 그대로 재사용** — 허브는 그 위에 얹는
  로그인/권한 게이트입니다.
- 탭 구성: `📈 주가` / `📋 공시` / `💰 자금`(하위: 차입·월간보고서·관리) / `🧓 퇴직연금`
  — 4개 탭 전부 실제 화면과 연결 완료(준비 중 placeholder 없음).
- 공용 모듈은 sys.path로 서로 다른 폴더의 코드를 그대로 import해서 재사용합니다(같은 로직을
  여러 곳에 베끼면 나중에 어긋나는 걸 방지):
  - `통합포털/app.py` → `차입금관리/report_shared.py`, `monthly_report_db.py` (월간보고서 렌더링/저장)
  - `주가현황/server.py` → `통합포털/danpan_mail.py`, `hub_db.py`, `mailer.py`, `hub_config.py`,
    `danpan_tracking_db.py` (단판공시 메일링 — 통합포털이 꺼져 있어도 독립적으로 동작)

## 핵심 파일 위치 요약

| 앱 | 폴더 | 포트 | 실행 방식 | 핵심 파일 |
|---|---|---|---|---|
| 주가/공시 | `ai 개발관련\주가현황` | 5000 | `python server.py` | `server.py`, `dashboard.js`, `styles.css`, `주가현황.html` |
| 자금(차입) | `ai 개발관련\차입금관리` | 8501 | `streamlit run app.py` | `app.py`, `report_shared.py`, `group_loan.py`, `interest_history_db.py`, `snapshot_db.py` |
| 퇴직연금 | `ai 개발관련\퇴직연금통합관리시스템` | 8000 | `python run_dashboard.py` | `run_dashboard.py`, `style.css`, `app.js` |
| 통합포털(허브) | `ai 개발관련\통합포털` | 9000 | `python app.py` | `app.py`, `hub_db.py`, `hub_config.py`, `mailer.py`, `danpan_mail.py`, `danpan_tracking_db.py`, `templates/*.html`, `static/style.css` |

## 현재 상태 요약 (분야별)

### 📈 주가
- 계열사·시멘트사 주가, 지수, 테마별 시세, 수급 동향, 주가 동향 코멘트(현재/장마감 기준),
  Export PDF, 자본금 자동조회 — 전부 완료·안정화.
- 통합포털 iframe 임베드용으로 자체 사이드바 메뉴(주가/공시 선택) 숨김 처리 완료.

### 📋 공시
- **단판공시**(단일판매·공급계약체결): DART 자동 스크랩 + 정기보고서 기준 진행현황 필터링,
  사전검증 계산기(유가증권시장 공시규정 제3조 — 4월 1일 기준 사업연도 롤오버 반영),
  **월별 자동/수동 메일링**(담당부서 표시 + 전월 대비 변동 이력 추적, 아래 이력 참고)까지 완료.
- **공정위 공시**(대규모내부거래): 특수관계인 출자·채권매도·상품용역거래(최초/변경) 3종 자동
  집계, 사전검증 계산기(비상장 자회사 5개사 포함), 20% 계열사 27개사 목록 자동 계산,
  공시기한(영업일) 준수 여부·공시대상 재확인 등 "정확성 점검" 기능까지 완료.
- **지분공시**: 임원·주요주주 소유상황보고 이력 집계 + 대량보유상황보고서(5% Rule),
  3필드(선임일/발행주식총수/주식수) 정확성 점검, 요청서류 체크리스트 완료.
- 세 카테고리 모두 "규정 보기"/"작성 가이드" 모달로 법령 원문·서식 안내 제공.

### 💰 자금
- 차입금현황(분기/월별 스냅샷, 상세+원형도표), 담보현황, 예상이자 계산(이자지급일 기준
  캘린더형, 금리변경 예정일 반영), 그룹차입금 분석(6개 법인, 연도별), 월간보고서(표지·
  자금수지·대여·운용·차입·어음현황 원본 재현 + A4 인쇄 최적화) 완료.
- 데이터 입력은 업로드/직접입력 이중 지원, 관리자 비밀번호 보호.

### 🧓 퇴직연금
- 대시보드 SQLite 전환, 도넛차트/톤 정리 완료.

### 🔐 계정·권한(통합포털)
- 로그인/접근승인/관리자 계정관리, 그룹웨어ID 기반 가입 + 메일 발송(실제 SMTP 연동 완료,
  사내 릴레이 `spamout.eugenes.co.kr`), 비밀번호 찾기, 내 정보 페이지.
- **RBAC 완료**: 팀별 화면 접근권한(자금팀=전체, 회계팀=주가·공시만 등) + 개인별 오버라이드
  (임원·팀장 등 팀 설정과 무관하게 예외 허용) — 우선순위는 관리자 > 개인설정 > 팀설정 > 기본허용.

## 남은 작업 (미착수)

- 차입금관리 월간보고서 엑셀 export(참고 서식에 맞춘 다운로드) — 현재는 조회/인쇄만 지원.
- 지분공시·공정위공시 전용 상세 기술문서(`공정위공시_모니터링.md` 등) 미작성 — 지금은 이
  문서의 개발 이력에만 기록되어 있음.
- 예상이자 계산 화면에 `interest_history_db.py`(과거 실적/확정 스케줄) 데이터를 실제로
  보여주는 UI 연동 — DB·파서·검증 로직은 완료됐으나 Streamlit 화면 연결은 진행 중.

---

## 개발 이력

날짜순으로 정리했습니다. 각 항목은 완료된 작업 기준이며, 더 자세한 배경/코드 위치는
괄호 안에 표시된 문서를 참고하세요.

### 2026-07-13 — 공시 포털 방향 확정, 공정위 공시 신규 구현

- 사내 그룹웨어 연동 → 기존 내부회계 포털(`tyia.vercel.app`) 편입(Supabase 이전) → **독립
  공시 포털** 순으로 세 번 방향이 바뀌다가, 최종적으로 "별도 배포 없이 지금 앱 위에서 계속
  기능을 쌓아가는" 방식으로 정리됨(이후 실제로는 통합포털 허브 방식으로 수렴).
- 공정위 공시(대규모내부거래) 최초 구현: 특수관계인 출자·채권매도·상품용역거래 3종 자동
  집계(`fetch_ftc_monitoring`), 사전검증 계산기(`check_ftc_disclosure`, 공정거래법 제26조·
  시행령 제33조 기준: 거래금액 100억 이상 또는 자본총계·자본금 중 큰 금액의 5%(최소 5억)).
- 임시로 주가현황 대시보드에 "공정위공시" 탭 추가해 시연·검증.

### 2026-07-14 — 공시 정확성 검증 도구 1단계, 지분공시 3필드 점검, 요청서류 체크리스트

- "공시 작성 가이드" 모달(서식 항목·기재상 유의사항·관련법규) 4개 공시유형에 추가.
- 지분공시 3필드(선임일·발행주식총수·주식수) 정확성 점검(`_equity_compute_accuracy`) —
  보고서 간 대조만으로 실제 오류 후보 2건 발견(선임일 연도 오탈자, 발행주식총수 절반 오기재).
- 요청서류 체크리스트(신규임원: 선임서+매매내역증빙+잔고증명서 / 변동: 매매내역서+잔고증명서),
  DART "보고구분" 필드로 자동 판별.
- 계열사+공시현황 축소판 레이아웃(계열사 선택 pill + 통합 공시 리스트) 구현.
- 비상장 자회사 5개사(금왕에프원 등)의 공정위 공시 기준 자본금·자본총계를 기업집단현황공시
  "회사 재무현황" 표에서 조회하는 방법 확립, 1,000억 기준 대형비상장주식회사 해당여부 조사.

### 2026-07-15 — 공정위 계산기 고도화, "20% 계열사" 목록 자동 계산

- 공시대상 재확인 근거 문장 구체화, 판정 결과 가시성(색상/대비) 버그 수정.
- 상품·용역거래 "20% 계열사" 요건을 체크박스 수동 판단 → 실제 DART 소유지분현황 표
  기반 자동 계산으로 전환. ROWSPAN 수백 행짜리 표를 펴는 `_expand_rowspan_grid()` 구현,
  20%이상 지분(A) + 그 50%초과 자회사(B, 상법 342조의2, 고정점까지 반복 확장) 알고리즘으로
  27개사 목록을 사용자 제시 목록과 정확히 일치시킴.
- 계열사 자본금·자본총계 조회를 5개사 개별 조회 → 대표회사 문서 하나로 전체 계열사(67개사)
  조회하도록 확장, 회사명 표기(㈜/주식회사/공백) 자동 정규화 매칭.

### 2026-07-16 — 상품·용역거래 "거래/변경" 구분, 조회기간 기준 변경

- report_nm이 "변경"으로 끝나는 사후공시(45일 특례, 이사회 의결 불요)와 "거래"(사전 이사회
  의결) 구분 처리.
- 공정위 공시 기본 조회기간을 롤링 365일 → "작년 1.1~오늘" 캘린더 연도 기준으로 변경.

### 2026-07-21 ~ 07-23 — RBAC 완성, 인터페이스 정리, 공정위 정확성 검증 마무리

- **팀별 화면 접근권한(RBAC)** 신규 구축: `team_permissions`/`user_permissions` 테이블,
  관리자 화면에서 팀별(예: 자금팀=전부, 회계팀=주가·공시만) + 개인별(임원·팀장 등 팀 설정과
  무관하게 예외 허용) 설정 가능. 우선순위: 관리자 > 개인설정 > 팀설정 > 기본 전체허용.
- 상품·용역거래 "변경(45일 특례)" 건의 실제 공시기한 판정 로직 구현(거래기간 필드에서
  분기 종료일 추출 + 45일), 옛 한글 날짜("2020년 4월 28일") 파싱 버그 수정.
- 자회사 4곳(동양에너지·금왕에프원·디씨아이티와이부천·인천피에프브이) 공정위 공시 통합 조회,
  `&cr;` 비표준 HTML 엔티티로 인한 라벨 매칭 실패 버그 수정(과거 다수 건에 영향).
- 공휴일 미반영 영업일 계산 버그, 다분기 묶음 문서의 비율 오매치 버그 수정.
- 그룹차입금보고서 기반 6개 법인(동양·동양에너지·금왕에프원·디씨아이티와이부천·인천피에프브이·
  유진마포130) 차입금 분석 신규 구축 — 연도별 탐색, 시각화 차트, 업로드 시 시트를 법인명으로
  매칭(위치 기반 X), 법인 자동감지로 드롭다운 오선택 차단, 억원/원 단위 혼재 자동 보정.
- 차입금관리 데이터를 분기 단위 → 월 단위로 전환.
- 예상이자 계산 페이지 신규 추가(월별/연별 보기, 이자지급일 기준 캘린더형, 금리변경 예정일
  반영, 영업일 조정).
- 통합포털 허브 자체(로그인/승인/탭), 관리자 계정 생성, 그룹웨어ID 기반 가입 + 메일 발송
  (초기엔 테스트모드), 비밀번호 찾기(인증코드), 내 정보 페이지 등 계정 시스템 구축.
- 통합포털에 자금(대여/운용/차입) 엑셀 서식을 그대로 조회+수정하는 기능 네이티브 구축,
  공정위 공시대상 검토(증가액 기준) 표시.
- 월간보고서를 표지·자금수지·대여·운용·차입·어음현황 구조로 원본 재현, 전체출력(표지부터)
  뷰, A4 가로 인쇄 스케일 맞춤 1차 적용.
- 실제 SMTP 릴레이(`spamout.eugenes.co.kr`, 인증서 기반, 계정 불필요) 연동 — 가입/비밀번호
  재설정 메일이 테스트모드에서 실제 발송으로 전환.

### 2026-07-28 — RBAC 개인화, 인쇄 안정화, 그룹차입금 버그 수정, 단판공시 메일링 완성

이 날 하루 동안 다음 작업을 순서대로 완료함(자세한 배경은 각 항목 옆 파일 참고):

1. **개인별 RBAC 오버라이드** 추가 — 팀 설정과 무관하게 특정 임원·팀장만 예외로 화면을
   열어줄 수 있게 `user_permissions` 테이블과 관리자 화면 추가.
2. **그룹차입금 데이터 버그 3종 수정**: (a) 같은 월에 다른 법인을 업로드하면 이전 파일이
   덮어써지던 문제 — `st.file_uploader` 위젯 키를 법인·연·월 조합으로 동적화해 해결.
   (b) 시트를 항상 "마지막 시트"로 가정하던 로직을 법인명 기준(`endswith` 매칭)으로 변경.
   (c) 업로드 파일과 선택한 법인이 다를 때 자동 감지 후 저장 차단, 단위(원/억원) 혼재 시
   100,000억원 초과 값을 자동으로 억원 환산.
3. **차입금관리 내부 "월간보고서" 메뉴 제거**, 대신 통합포털 허브의 네이티브 월간보고서에
   "차입"(차입금 및 담보현황) 서브탭으로 통합 — 중복 메뉴 정리.
4. **주가현황 장마감 기준 인쇄 라벨** — "(장마감기준, YYYY-MM-DD)"가 인쇄 화면에도 보이도록
   `document.title`뿐 아니라 화면 요소에도 명시.
5. **단판공시 사전검증 기준연도 계산 수정** — 유가증권시장 공시규정 제3조(적용기준: 매
   사업연도 종료 후 3월 경과일부터 다음 사업연도 종료 후 3월까지 적용)를 그대로 구현.
   이전엔 "가장 최근 제출된 사업보고서"를 무조건 쓰다 보니 미래 계약일자를 넣으면 항상
   오늘 기준 최신 보고서로 폴백해 틀린 답을 주던 문제를 해결 — 이제 계약일자로부터
   4월 1일 롤오버 규칙에 따라 "이론적으로 필요한 사업연도"를 먼저 계산한 뒤, 그 연도
   사업보고서가 실제로 제출됐는지 확인해서 없으면 "아직 판단 불가"로 명확히 응답.
6. **월간보고서 인쇄 화면 정리** — 인쇄 시 서브내비/표지 제목 라벨 숨김, `--print-scale`
   CSS 커스텀 속성을 페이지 로드 시점에 계산해두는 방식으로 전환(이전엔 `beforeprint` 이벤트
   의존이라 인쇄 경로에 따라 스케일이 안 걸리는 사례가 있었음), 인쇄 여백/셀 패딩 축소.
7. **어음현황 등 시트 인쇄 깨짐 버그 수정** — `report_shared.render_sheet_html()`이 시트의
   `print_area`를 무시하고 전체 사용범위(`max_row`/`max_column`)를 그리는 바람에, 인쇄영역
   밖 여분의 열(예: 어음현황 시트는 인쇄영역 A1:T31인데 실제 사용범위는 AA열까지 있음)까지
   같은 표에 섞여 화면이 깨져 보였음. 인쇄영역이 있으면 그 범위만 렌더링하도록 수정하고,
   엑셀에 이미 사람이 맞춰둔 인쇄배율(`page_setup.scale`, 시트마다 42~95% 사이 실제 값 존재)을
   `data-excel-scale` 속성으로 실어보내 프런트가 자체 계산 대신 그 값을 그대로 쓰도록 변경.
8. **단판공시 월별 메일링 시스템 신규 구축** — 처음엔 "월별 현황을 보고하는 메일"로
   시작했으나, 실제 목적이 "담당부서에 이번 달 변동내역을 요청"하는 것으로 재정의되어
   다음과 같이 완성:
   - 발신자는 박시환(자금팀) 계정 고정(`hub_config.MAIL_FROM_DANPAN`), 수신자는 관리자
     화면(`/admin/danpan-mail`)에서 자유롭게 추가/삭제.
   - 메일은 "안녕하세요, 자금팀 {발신자}입니다. / 당월 단일판매·공급계약 변동내역
     요청드립니다."로 시작하는 HTML 표 형식 — 현장별 담당부서(관리자가 직접 입력,
     `danpan_tracking_db.site_meta`)·계약상대방·금액·공사기간·최근공시(차수)를 표로 정리.
   - **월별 스냅샷 비교(이력관리)**: 발송할 때마다 그 시점 현장 목록을 저장해두고
     (`danpan_monthly_snapshot`), 다음 달 발송 시 직전 스냅샷과 자동 비교해 금액 변경·
     공사기간 변경·정정공시·신규등록·관리종료(목록에서 빠짐)를 표에서 노란색으로 강조.
   - 매월 1일 자동 발송(주가현황 서버 내 상시 백그라운드 스레드, 이미 발송한 달은
     `danpan_mail_log`로 중복 방지) + 단판공시 화면 우측 상단 "📧 메일 보내기" 버튼으로
     즉시 수동 발송도 가능.
   - 발송 로직(`통합포털/danpan_mail.py`)은 통합포털·주가현황 양쪽에서 그대로 import해
     재사용 — 주가현황 서버가 통합포털 프로세스 없이도 독립적으로 메일을 보낼 수 있음.
9. **예상이자 계산 개선 착수** — 팀에서 수기 관리하던 "28일 이자.xlsx"(연도별 실제 정산이자
   내역 + PCBO처럼 만기까지 확정된 고정 스케줄 대출)를 분석. 파일이 사람이 수기로 관리해온
   표라 같은 시트 안에서도 구간(월)마다 컬럼 구성(이율·적수 유무)이 달라, 헤더를 한 번만
   찾지 않고 매 구간마다 다시 인식하도록 파서를 작성하고, 재계산값과 10% 넘게 어긋나는
   행은 자동으로 걸러내는 검증 로직을 추가(`interest_history_db.py`). 과거 실적 조회 +
   확정 스케줄 반영을 목표로 DB·파서까지 완료, Streamlit 화면 연동은 진행 중.

---

## 관련 문서

| 문서 | 내용 |
|---|---|
| [`단판공시_모니터링.md`](단판공시_모니터링.md) | 단판공시 기능 상세(코드/API/사전검증 계산기) |
| [`지분공시_모니터링.md`](지분공시_모니터링.md) | 지분공시·5% Rule 기능 상세 |
| [`데이터_출처.md`](데이터_출처.md) | 주가현황 화면 각 숫자의 정확한 API 엔드포인트 정리 |
| [`README.md`](README.md) | 주가현황 대시보드 전체 개요 |
